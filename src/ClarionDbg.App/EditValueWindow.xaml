<Window x:Class="ClarionDbg.App.EditValueWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Value" Height="180" Width="420" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{StaticResource Panel}" Foreground="{StaticResource Fg}" FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TxtName" Grid.Row="0" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,4"/>
        <TextBlock x:Name="TxtInfo" Grid.Row="1" Foreground="{StaticResource FgDim}" FontSize="12" Margin="0,0,0,10"/>
        <TextBox x:Name="TxtValue" Grid.Row="2" Padding="6,5" FontFamily="Consolas" FontSize="13"
                 Background="{StaticResource Bg}" Foreground="{StaticResource Fg}"
                 BorderBrush="{StaticResource Accent}" BorderThickness="1"
                 KeyDown="TxtValue_KeyDown"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="OK" Width="90" Click="Ok_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="90" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

<Application xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Professional dark theme (no purple) -->
        <Color x:Key="BgColor">#FF1E1E1E</Color>
        <SolidColorBrush x:Key="Bg" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="Panel" Color="#FF252526"/>
        <SolidColorBrush x:Key="PanelAlt" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="Border" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="Fg" Color="#FFD4D4D4"/>
        <SolidColorBrush x:Key="FgDim" Color="#FF858585"/>
        <SolidColorBrush x:Key="Accent" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="AccentLight" Color="#FF3794FF"/>
        <SolidColorBrush x:Key="CurLine" Color="#FF264F78"/>
        <SolidColorBrush x:Key="BpRed" Color="#FFE51400"/>
        <SolidColorBrush x:Key="Ok" Color="#FF4EC9B0"/>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PanelAlt}"/>
            <Setter Property="Foreground" Value="{StaticResource Fg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Accent}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Header" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource FgDim}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="8,6,8,4"/>
        </Style>
    </Application.Resources>
</Application>
